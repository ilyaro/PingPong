version: "3.8"
services:
  ping:
    image: gfish/ping
    build:
      context: .
      args:
        PORT: 8089
        PARTNER: pong
    ports:
      - ${outport1}:${inport}
  pong:
    image: gfish/pong
    build:
      context: .
      args:
        PORT: 8089
        PARTNER: ping
    ports:
      - ${outport2}:${inport}
