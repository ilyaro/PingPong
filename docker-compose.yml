version: "3.8"
services:
  pingpong1:
    image: gfish/pingpong
    command:  [ "app/hello.py", ${inport} ]
    ports:
      - "${outport1}:${inport}"
  pingpong2:
    image: gfish/pingpong
    command: [ "app/hello.py", ${inport} ]
    ports:
      - "${outport2}:${inport}"
