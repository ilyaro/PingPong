version: "3.8"
services:
  ping:
    image: gfish/ping
    build:
      context: .
      environment:
        - PARTNER=pong
    ports:
      - ${outport1}:${inport}
  pong:
    image: gfish/pong
    build:
      context: .
      environment:
        - PARTNER=ping
    ports:
      - ${outport2}:${inport}
