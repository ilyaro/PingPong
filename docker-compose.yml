version: "3.8"
services:
  pingpong1:
    image: gfish/pingpong
    build:
      context: .
      args:
        - PORT=${inport}
    ports:
      - ${outport1}:${inport}
  pingpong2:
    image: gfish/pingpong
    ports:
      - ${outport2}:${inport}
