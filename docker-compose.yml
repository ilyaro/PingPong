version: "3.8"
services:
  ping:
    image: gfish/pingpong
    environment:
      - PARTNER=pong
    build:
      context: .
    ports:
      - ${outport1}:${inport}
  pong:
    image: gfish/pingpong
    environment:
      - PARTNER=ping
    ports:
      - ${outport2}:${inport}
