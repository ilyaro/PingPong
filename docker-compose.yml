version: "3.8"
services:
  ping:
    image: gfish/ping
    build:
      context: .
      args:
        - PORT=${inport}
        - PARTNER=pong
    ports:
      - ${outport1}:${inport}
  pong:
    image: gfish/pong
    build:
      context: .
      args:
        - PORT=${inport}
        - PARTNER=ping
    ports:
      - ${outport2}:${inport}
